# Current manuscript analysis and license-free verification.
# Recorded environment: CPython 3.10.12. Solvers are deliberately optional.
numpy==2.2.6
scipy==1.15.3
pandas==2.3.3
matplotlib==3.10.9
pytest==9.0.3

# Fresh HiGHS experiment only: python3 -m pip install highspy==1.15.1
# Fresh Gurobi arm (license required): python3 -m pip install gurobipy==13.0.2
# PDF compilation additionally needs pdfLaTeX, BibTeX and standard TeX packages.
# C++ export integration additionally needs GNU Make and g++ with C++17 support.
