# Build artifacts
code/build/
code/build-cbc/
*.o
*.d
# LaTeX intermediates
paper/*.aux
paper/*.log
paper/*.bbl
paper/*.blg
paper/*.out
paper/*.spl
paper/*.toc
paper/*.fls
paper/*.fdb_latexmk
# Analysis outputs (regenerated by `make analysis-n0`)
results-analysis/
# Synthetic smoke outputs
code/synthetic/resultados/
# External heavy artifacts (distributed via Release/Zenodo, not git)
*.tar.zst
# OS / editor
.DS_Store
*~
# Wiley NJD v5 template fonts (commercial: Univers/Myriad/MinionPro/etc.).
# Not committed; obtain from the official Wiley NJD v5 template to reproduce typography.
paper/Fonts/

# LaTeX page-tracking artifacts of the Wiley class
paper/*.pag
# Python bytecode
scripts/__pycache__/
# alternate build dirs
code/build*/

# CPLEX flat-control per-instance CSVs (bulky; analysis only needs resumen.csv)
results-revision/plano-control-cplex/**/resultados/
results-revision/plano-control-cplex/**/logs/

# Solver-robustness campaign: raw staging (historical logs are archived as a tarball)
results-revision/solver-robustness/inventory/raw/
results-revision/solver-robustness/runs-raw/
code/build-robust/
